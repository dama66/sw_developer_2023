<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Swd.TimeManager.GuiMaui.Views.ProjectDeletePage"
             xmlns:viewmodel="clr-namespace:Swd.TimeManager.GuiMaui.ViewModel"
             Title="ProjectDeletePage">
    
    <ContentPage.BindingContext>
        <viewmodel:ProjectDeletePageViewModel />
    </ContentPage.BindingContext>

    <VerticalStackLayout>
        <Grid Margin="10" x:Name="editGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Text="Projektname" Margin="5"/>
            <Label Grid.Row="1" Margin="5" Text="{Binding Project.Name, Mode=TwoWay}" />
        </Grid>
        <Button Text="Delete project" Margin="5" Command="{Binding DeleteCommand}" />
        <Button Text="Cancel" Margin="5" Command="{Binding CancelCommand}" />

    </VerticalStackLayout>
</ContentPage>